# Migration `20200902161555-add_unique_tags_to_roles_table`

This migration has been generated by Vasco Martins at 9/2/2020, 5:15:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX `Role.name_unique` ON `meet`.`Role`(`name`)

CREATE UNIQUE INDEX `Role.slug_unique` ON `meet`.`Role`(`slug`)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200902160947-create_roles_table..20200902161555-add_unique_tags_to_roles_table
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model Course {
   id   Int    @default(autoincrement()) @id
@@ -15,10 +15,10 @@
 }
 model Role {
   id       Int    @default(autoincrement()) @id
-  name     String
-  slug     String
+  name     String @unique
+  slug     String @unique
   priority Int    @default(0)
   User     User[]
 }
@@ -35,5 +35,6 @@
   Course    Course?  @relation(fields: [courseId], references: [id])
   Role      Role     @relation(fields: [roleId], references: [id])
   @@index([courseId], name: "courseId")
+
 }
```


